import 'package:flutter/material.dart';

class MetricConversion extends StatelessWidget {
  const MetricConversion({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Metric Conversion.."),
        centerTitle: true,
      ),
    );
  }
}